```
┌─────────────────────────────────────────────────────────┐
│                  TRIGGER (실행 방법)                      │
├──────────────────────┬──────────────────────────────────┤
│   수동 (로컬 PC)      │        자동 (클라우드)             │
│                      │                                  │
│  /check-docs 입력    │   GitHub Actions                 │
│  또는                 │   매일 오전 9시 KST              │
│  uv run python       │   (PC 꺼져있어도 동작)             │
│    main.py           │                                  │
│    --send-email      │                                  │
└──────────┬───────────┴──────────────┬───────────────────┘
           │                          │
           ▼                          ▼
┌─────────────────────────────────────────────────────────┐
│              Step 0: URL 목록 자동 갱신                    │
│                                                         │
│   sitemap.xml ──fetch──► 새 페이지 있나? ──YES──► urls.json│
│   (code.claude.com)       │                    업데이트   │
│                          NO                             │
│                           │                             │
│                     urls.json 유지                       │
│                     (현재 75개)                           │
└────────────────────────────┬────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────┐
│              Step 1: 문서 Fetch                          │
│                                                         │
│   urls.json의 75개 URL ──httpx──► HTML 파싱              │
│                                      │                  │
│                                      ▼                  │
│                              snapshots/ 디렉토리에 저장    │
│                              (overview.txt, setup.txt...)│
└────────────────────────────────┬────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────┐
│              Step 2: Diff 비교                           │
│                                                         │
│   이전 커밋 (git)         현재 snapshots/                 │
│   ┌──────────┐           ┌──────────┐                   │
│   │overview  │           │overview  │                   │
│   │setup     │◄──diff──►│setup     │                   │
│   │hooks     │           │hooks     │                   │
│   │...       │           │...       │                   │
│   └──────────┘           └──────────┘                   │
│                    │                                    │
│          ┌────────┴────────┐                            │
│          ▼                 ▼                            │
│     변경 없음          변경 있음                          │
│     (종료)            (Step 3으로)                       │
└───────────┬────────────────┬────────────────────────────┘
            │                │
            ▼                ▼
┌──────────────┐  ┌──────────────────────────────────────┐
│  "변경사항    │  │     Step 3: HTML 리포트 생성 & 발송     │
│   없습니다"   │  │                                      │
│              │  │  ┌─────────────────────────────────┐  │
└──────────────┘  │  │  [Claude Code Docs Change Report]│  │
                  │  │  2026-03-31                      │  │
                  │  │                                  │  │
                  │  │  +2 added | ~3 modified | -1 del │  │
                  │  │                                  │  │
                  │  │  MODIFIED ── overview             │  │
                  │  │  - old line                      │  │
                  │  │  + new line                      │  │
                  │  └─────────────────────────────────┘  │
                  │               │                      │
                  │               ▼                      │
                  │     Gmail SMTP ── 직접 발송            │
                  │     To: qkrtjdgh751014@gmail.com     │
                  └──────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────┐
│              Step 4: 스냅샷 저장                          │
│                                                         │
│   git add snapshots/ ──► git commit ──► git push        │
│                                        (GitHub Actions) │
│                                                         │
│   다음 실행 시 이 커밋이 "이전 버전" 기준이 됨              │
└─────────────────────────────────────────────────────────┘


┌─────────────────── 프로젝트 구조 ───────────────────────┐
│                                                         │
│  C:\dev\detect_ccd_change\                              │
│  ├── .github/workflows/check-docs.yml  ← GitHub Actions │
│  ├── .claude/skills/check-docs/SKILL.md ← /check-docs  │
│  ├── .env                  ← 로컬 설정 (gitignore)       │
│  ├── .env.example          ← 설정 템플릿 (공개)          │
│  ├── urls.json             ← 모니터링 대상 URL 목록       │
│  ├── main.py               ← 메인 실행 스크립트           │
│  ├── snapshots/            ← 문서 스냅샷 (git 관리)       │
│  │   ├── overview.txt                                   │
│  │   ├── setup.txt                                      │
│  │   └── ... (75개)                                     │
│  └── scripts/                                           │
│      ├── update_urls.py    ← sitemap에서 URL 자동 갱신   │
│      ├── fetch_docs.py     ← 문서 가져오기               │
│      ├── diff_docs.py      ← 이전 vs 현재 비교           │
│      ├── generate_email.py ← HTML 이메일 생성            │
│      └── send_email.py     ← Gmail SMTP 발송            │
│                                                         │
│  GitHub: Pakkoc/detect-ccd-change (public)              │
│  Secrets: EMAIL_TO, GMAIL_USER, GMAIL_APP_PASSWORD      │
└─────────────────────────────────────────────────────────┘
```
